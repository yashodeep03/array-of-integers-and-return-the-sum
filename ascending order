#include<iostream>
#include<vector>
#include<algorithm>
int main()
{
std::vector<int>numbers={10,23,4,12};
std::sort(numbers.begin(),numbers.end());
std::cout<<"sorted numbers:";
for(int num:numbers)
{
std::cout<<num<<"";
}
return 0;
}
